interface:
  display_name: "Novel Index"
  short_description: "Build and query a lightweight project index."
  default_prompt: "Use $novel-index to build or query a lightweight index for the current novel project."

policy:
  allow_implicit_invocation: true
