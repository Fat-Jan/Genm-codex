# v1.4-roadmap Review 报告（2026-03-28）

## 审查范围

审查 `v1.4-roadmap.md` 中 owner 为 `[me]`（即 gpt）的所有任务，对比 roadmap 描述与代码库实际情况。

---

## 一、状态不一致（需要更新 roadmap）

| 任务 | Roadmap 状态 | 实际验证 | 建议更新为 |
|------|-------------|---------|-----------|
| A4 | `[in_progress]` | ✅ 基本完成 | `[done]` |
| A5 | `[planned]` | ⚠️ 已有实现 | `[in_progress]` |
| C2 | `[planned]` | ⚠️ 已有完整实现 | `[done]` |
| C3 | `[planned]` | ⚠️ 已有实现 | `[done]` |
| C4 | `[planned]` | ⚠️ 已有实现 | `[done]` |
| D3 | `[planned]` | ⚠️ 已有完整接线 | `[done]` |
| E1/E2/E3 | `[planned]` | ⚠️ 部分完成 | `[in_progress]` |

### 具体证据

**A4**: `project_knowledge_mcp_server.py` 已调用 `render_workflow_health_summary`，满足"至少 1 个真实消费端复用 helper"

**A5**: `audit_project_quality_state.py` 已实现，检查了 `issue_clusters`、`dimension_scores`、`anti_flattening_*`、`recommended_next_action`

**C2**: `audit_chapter_structure_repetition.py` + `test_chapter_structure_repetition.py` 已存在，被 `project_regression_smoke.py` 调用

**C3**: `audit_precheck_structure_risks.py` 已存在，`novel-precheck/SKILL.md` 已提及 `chapter_structure` 检查

**C4**: `projects/成婚前三日，我先改了侯府嫁妆单/` 已存在，`project_regression_smoke.py` 已调用相关审计

**D3**: `build_content_positioning.py` 已调用 `resolve_platform_positioning()`，`project-maintenance.py` 和 `novel-init/SKILL.md` 已集成

---

## 二、非状态类问题

### 问题 1：脚本名称不一致（A5）

| 位置 | 写的是 | 实际是 |
|------|--------|--------|
| roadmap A5 | `quality-audit.py` | `audit_project_quality_state.py` |

**影响**：按 roadmap 找文件会找不到。

**建议**：修正 roadmap 中的脚本名称。

---

### 问题 2：文档缺口被低估（A1）

Roadmap 说"仍需继续把 schema 粒度和 docs 说明拉齐"，语气像是小收尾工作。

**实际差距**：
- `state-schema.md` 示例 JSON 只有约 10 个顶层字段
- 实际 schema/template 有 27 个顶层字段
- 缺少的 17 个字段完全没有结构定义或说明：
  - `meta`, `progress`, `quality_metrics`, `auto_learn_config`, `platform_config`
  - `genre_profile`, `active_launch_grammar`, `active_primary_pivot`
  - `launch_stack_phase`, `launch_stack_drift_signal`
  - `active_context`, `chapter_meta`, `character_states`, `setting_versions`
  - `dungeons`, `teammates`, `constraints_loaded`

**这不是"拉齐"，是"大面积缺失"。**

**建议**：
1. 在 A1 的完成标记中明确要求更新 `state-schema.md` 的示例 JSON 和字段说明表
2. 或者将文档更新拆成独立任务

---

### 问题 3：缺少验证命令（A1-A5）

Roadmap 要求完成后补充 `Verify` 信息，但以下任务缺失：

| 任务 | Verify |
|------|--------|
| A1 | ❌ 无 |
| A2 | ❌ 无 |
| A3 | ❌ 无 |
| A4 | ❌ 无 |
| A5 | ❌ 无 |

**影响**：无法确认这些任务是否真的能跑通。

**建议**：补充具体的验证命令，例如：
- A1: `pytest tests/test_state_contracts.py`
- A2: `pytest tests/test_thin_state_atomic.py`
- A3: `pytest tests/test_project_maintenance_tolerance.py`
- A4: `pytest tests/test_workflow_health_bundle.py tests/test_workflow_health_render.py`
- A5: `pytest tests/test_project_quality_audit.py`

---

### 问题 4：完成标记模糊（A4）

Roadmap A4 的完成标记是：
> "至少 1 个真实消费端不再自己拼字段，而是复用 helper"

**实际情况**：
- `project_knowledge_mcp_server.py` 调用了 `render_workflow_health_summary` ✅
- `novel-status` 和 `novel-query` 只是"读取 JSON 文件"，并没有调用 helper ❓

**问题**：什么算"复用 helper"？读 JSON 算不算？

**建议**：明确完成标准。如果读 JSON 也算消费，那 A4 已完成；如果必须调用 helper 函数，需要进一步确认。

---

### 问题 5：自动化程度定义不清（E1/E2/E3）

Roadmap 描述：
- E1: `e2e-novel` 自动 smoke
- E2: 《嫁妆单》自动 regression smoke
- E3: workflow-health / quality-audit / projection 总结 smoke

**实际情况**：
- 存在 `project_regression_smoke.py`，但它是手动运行的脚本
- 存在多个 smoke 脚本（`writing_core_smoke.py`、`fanqie_p0_smoke.py` 等）
- 没有找到 CI/自动触发机制

**问题**："自动 smoke"到底指什么？
- 是"有脚本可以跑"？
- 还是"有 CI 自动触发"？
- 还是"有单一命令可以跑全部"？

**建议**：明确 E1/E2/E3 的完成标准。

---

### 问题 6：完成标准不一致（A1）

Roadmap A1 的完成标记是：
> "schema / template / docs / tests 一致"

**实际情况**：
- schema / template / tests ✅ 一致
- docs ❌ 不一致（示例 JSON 严重过时）

**问题**：docs 不一致，但任务仍标记为 `[in_progress]` 而不是 `[blocked]`。这是否意味着 docs 不在 A1 的范围内？

**建议**：明确 A1 的范围。如果 docs 在范围内，需要更新完成标记；如果不在范围内，需要说明原因。

---

## 三、总结

### 需要 gpt 解释/解决的问题

| # | 问题 | 涉及任务 | 类型 |
|---|------|---------|------|
| 1 | 状态过时（7 个任务） | A4, A5, C2, C3, C4, D3, E1-E3 | 更新 roadmap |
| 2 | 脚本名称不一致 | A5 | 修正 roadmap |
| 3 | 文档缺口被低估 | A1 | 明确范围/拆分任务 |
| 4 | 缺少验证命令 | A1-A5 | 补充 Verify |
| 5 | 完成标记模糊 | A4 | 明确标准 |
| 6 | 自动化定义不清 | E1-E3 | 明确标准 |
| 7 | 完成标准不一致 | A1 | 明确范围 |

### 建议处理顺序

1. **立即处理**：#1（状态更新）、#2（脚本名称）、#4（验证命令）—— 直接改 roadmap
2. **需要讨论**：#3（文档缺口）、#5（完成标记）、#6（自动化定义）、#7（完成标准）—— 需要 gpt 解释或明确

---

## 四、附件

### 验证命令参考

```bash
# A1: state schema / template 一致性
pytest tests/test_state_contracts.py -v

# A2: thin-state.py 原子写入
pytest tests/test_thin_state_atomic.py -v

# A3: 维护链容错
pytest tests/test_project_maintenance_tolerance.py -v

# A4: workflow-health 接入
pytest tests/test_workflow_health_bundle.py tests/test_workflow_health_render.py tests/test_workflow_health_smoke.py -v

# A5: 质量 artifact 写回
pytest tests/test_project_quality_audit.py -v

# C2: 相邻章结构重复扫描
pytest tests/test_chapter_structure_repetition.py -v

# 全量验证
bash scripts/validate-migration.sh
pytest -q
```
