# Smoke 目录说明

## 保留策略

- **基线样本**：不带日期后缀的目录长期保留
- **派生副本**：带日期或框架后缀的目录只保留仍被结论文档引用的版本
- **清理**：同一路线出现更新副本后，旧派生副本应归档或删除，避免无限堆积

## 命名建议

- 基线样本：`e2e-<route>`
- 派生副本：`e2e-<route>-<framework>-<date>`

## 使用边界

- `smoke/` 里的副本用于验证，不是默认写作工作目录
- 新增 smoke 结论文档时，应明确引用哪个副本
