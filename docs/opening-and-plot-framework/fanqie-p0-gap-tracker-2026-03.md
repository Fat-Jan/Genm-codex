# Fanqie P0 缺口追踪（2026-03）

## 目标

把当前 `fanqie_p0_smoke.py` 已覆盖的真实桶、仍然缺样本的桶，以及下一批最值得补的真实项目样本统一收束成可执行清单。

这份文档不是重新解释框架，而是给后续会话一个直接抓手，避免每次都重新盘点：

- 哪些桶已经有真实项目 smoke
- 哪些桶已经有真实项目轻量 writeback
- 哪些桶还停留在 overlay / checkcard 层
- 下一批最该补什么

## 已覆盖桶

当前已经有真实项目 smoke + `chapter_meta["003"]` 轻量 writeback 的桶：

- `宫斗宅斗`
- `青春甜宠`
- `都市脑洞`
- `职场婚恋`
- `都市日常`
- `玄幻脑洞`
- `历史脑洞`
- `豪门总裁`

这些桶当前都已经有：

- 真实项目样本
- bucket 级 smoke 文档
- `fanqie_bucket_flags / fanqie_bucket_summary` 写回样本
- `tests/test_fanqie_p0_smoke.py` 与 `tests/test_opening_plot_framework.py` 的回归约束

## 仍缺真实项目的桶

当前 `P0 8 桶` 已全部拿到至少 2 条真实项目样本，其中：

- `历史脑洞` 已有第 2 条真实样本
- `豪门总裁` 已有第 2 条真实样本
- `都市脑洞` 已有第 2 条真实样本
- `玄幻脑洞` 已有第 2 条真实样本
- `青春甜宠` 已有第 2 条真实样本
- `职场婚恋` 已有第 2 条真实样本
- `都市日常` 已有第 2 条真实样本

当前不再缺第二条样本，后续若继续推进，重点应转成第三条样本和不同路数样本。

原因：

- 当前双样本覆盖已完成
- 若继续推进，更值得补的是明显不同路数的第三条样本，而不是重复同模板

## 下一批推荐顺序

1. 第三条 `都市脑洞`
2. 第三条 `豪门总裁`
3. 第三条 `玄幻脑洞`
4. 第三条 `历史脑洞`

推荐逻辑：

- 先补更能影响 `confidence` 门槛判断的第三条样本
- 优先补与现有样本路数差异更大的桶
- 不优先继续机械重复同模板样本

## 可直接开工的项目提示词

### 历史脑洞

```text
请在 `/Users/arm/Desktop/vscode/Genm-codex/projects/我在县衙当杂吏，靠翻旧案升了堂` 里启动一个番茄 `历史脑洞` 项目，按最小可跑真实样本做：

1. 用 `novel-init` 初始化项目
2. 用 `novel-outline` 生成总纲和第001-003章章纲
3. 直接写出第001-003章正文
4. 风格要求：
   - 第一章直接落身份困局和制度压力
   - 脑洞点要尽快亮出，并立刻影响现实处境
   - 第2章必须出现第一次“脑洞/机制见效”
   - 第3章必须形成一次小闭环，但不能把主线冲突打穿
   - 不许大段讲历史设定代替剧情
5. 完成后：
   - 不要扩写到第4章以后
   - 给我一个适合跑 `fanqie_p0_smoke.py` 的章节范围建议
   - 说明这个项目为什么能验证 `历史脑洞` bucket
```

### 豪门总裁

```text
请在 `/Users/arm/Desktop/vscode/Genm-codex/projects/签下离婚协议那天，冷脸总裁改口叫我合伙人` 里启动一个番茄 `豪门总裁` 项目，按最小可跑真实样本做：

1. 用 `novel-init` 初始化项目
2. 用 `novel-outline` 生成总纲和第001-003章章纲
3. 直接写出第001-003章正文
4. 风格要求：
   - 第一章直接给资源压制和关系错位
   - 男女主第一次同场就带现实利害，不许只靠脸和气氛
   - 第2章必须出现一次高压互动升级
   - 第3章必须给出一次可感的小兑现
   - 不许写成只剩身份标签的空壳总裁文
5. 完成后：
   - 不要扩写到第4章以后
   - 给我一个适合跑 `fanqie_p0_smoke.py` 的章节范围建议
   - 说明这个项目为什么能验证 `豪门总裁` bucket
```

## 当前判断

下一步如果继续推进默认工作流，不该先做：

- `v1.2` 的功能膨胀
- 多 bucket 混判
- 联网市场信号自动接入

更值钱的是继续补第二条真实样本，把当前 `medium confidence` 门槛打稳。
