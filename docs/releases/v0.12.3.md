# Genm-codex v0.12.3

第八阶段 project-annotate 实验增强完成版发布。

## 目的

这个版本用于把第八阶段最新的实验性增强正式落到远端可追踪版本上，并补齐：

- `novel-scan` 的 `project-annotate` 路径
- `.mighty/state.json -> market_adjustments` 的项目级建议写入
- 对 `shared/profiles` 保持零默认改动的边界

## 说明

- `v0.12.2` 已存在，但本次 `v0.12.3` 是第八阶段 project-annotate 完整落盘后的正式完成版
- 不重写既有 tag 历史，采用非破坏式补丁版本推进

## 当前能力

到 `v0.12.3`，`Genm-codex` 已完成：

- `novel-scan` 的 report-only 实验版
- `novel-scan` 的 project-annotate 实验版
- `shared` 同步的 machine-readable 可观测输出
