# Genm-codex v0.16.0

## Summary

`v0.16.0` marks the Phase 12 readiness checkpoint:

- `v1.0.0` readiness checklist
- `v1.0.0` readiness assessment

## Highlights

### Formal readiness checklist

Added:

- `docs/90-归档/迁移与RC/v1-readiness-checklist.md`

This turns the `1.0` discussion into explicit gate checks instead of informal judgment.

### Formal readiness assessment

Added:

- `docs/90-归档/迁移与RC/v1-readiness-assessment.md`

Current conclusion:

- no hard blocker was identified
- but the project should still go through an RC-style boundary before a direct `v1.0.0`

### Product maturity impact

By this point, `Genm-codex` has:

- a migrated capability base
- default workflows
- explicit experimental boundaries
- a documented path toward RC and 1.0

## Recommendation

The next meaningful step after this release is not another large migration phase.

It is:

- deciding whether to enter `v1.0.0-rc1`

## Notes

`novel-scan` remains experimental and does not block the current default workflow.
