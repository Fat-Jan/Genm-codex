# Genm-codex Phase 4A / P0 Fix Smoke Plan

## 目标

验证 `novel-fix` 第一版的最小可用性。

项目目录统一使用：

- `/Users/arm/Desktop/vscode/Genm-codex/e2e-novel`

## 测试约束

- 只允许操作目标项目目录
- 如果上下文不是目标目录，则停止
- 这轮允许修改目标章节与必要的状态元数据
- 不应无理由升级成完整 `rewrite`

## 1. `novel-fix`

### 测试目标

验证它能基于 review 结果做一轮小范围定向修复，而不是把任务做成“重写整章”。

### 推荐提示词

```text
硬约束：只允许操作这个项目目录
/Users/arm/Desktop/vscode/Genm-codex/e2e-novel

如果你当前上下文不是这个目录，先切换到这个目录，再继续。
如果不能确认自己正在读取这个目录，就停止，不要继续。

在确认目录正确后，再使用 novel-fix skill，对第001章执行一次定向修复。

要求：
1. 只基于当前 review 结果，优先修最关键的两项问题
2. 不要把这次任务升级成完整 rewrite
3. 保存修复结果，并更新 `.mighty/state.json`
4. 如果需要同步相关设定文件，可以做最小同步，但必须在结果中说明

完成后只汇报：
- 实际操作的项目根目录
- 是否成功保存修复后的第001章
- 主要修了哪两项问题
- 更新了哪些 state 字段
- 是否改动了除章节正文外的其他文件
- 你为什么判定这次属于 fix 而不是 rewrite
```

### 通过标准

- 确认正确项目目录
- 成功保存修复后的 `第001章`
- 修复目标明确对应 review 问题
- 更新合理的 fix 元数据
- 不无端升级成完整 rewrite
