# Genm-codex Phase 4A / P1 Precheck Smoke Plan

## 目标

验证 `novel-precheck` 第一版的最小可用性。

项目目录统一使用：

- `/Users/arm/Desktop/vscode/Genm-codex/e2e-novel`

## 测试约束

- 只允许操作目标项目目录
- 如果上下文不是目标目录，则停止
- 默认走只读预检，不修改任何项目文件

## 1. `novel-precheck`

### 测试目标

验证它能对指定章节范围做一份可信的投稿前预检报告，而不是空泛复述平台规则。

### 推荐提示词

```text
硬约束：只允许操作这个项目目录
/Users/arm/Desktop/vscode/Genm-codex/e2e-novel

如果你当前上下文不是这个目录，先切换到这个目录，再继续。
如果不能确认自己正在读取这个目录，就停止，不要继续。

在确认目录正确后，再使用 novel-precheck skill，对第001章到第003章执行一次番茄平台投稿前预检。

要求：
1. 优先读取：
   - `.mighty/state.json`
   - `chapters/第001章.md`
   - `chapters/第002章.md`
   - `chapters/第003章.md`
2. 如有必要，再参考对应 profile
3. 输出至少包含：
   - 综合评估
   - 必须修复
   - 建议修复
   - 平台适配判断
   - 投稿建议
4. 不要改动任何文件

完成后只汇报：
- 实际操作的项目根目录
- 预检报告的主要结论
- 你最终给的是“可以投稿 / 需要修改后投稿 / 不建议投稿”中的哪一个
- 是否改动了任何文件
```

### 通过标准

- 确认正确项目目录
- 真正结合章节内容给出判断
- 能区分必须修复与建议修复
- 能给出明确投稿建议
- 不修改任何文件
