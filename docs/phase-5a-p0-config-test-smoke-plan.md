# Genm-codex Phase 5A / P0 Config & Test Smoke Plan

## 目标

验证 `novel-config` 与 `novel-test` 第一版的最小可用性。

项目目录统一使用：

- `/Users/arm/Desktop/vscode/Genm-codex`

## 测试约束

- 只允许操作目标项目目录
- 如果上下文不是目标目录，则停止
- 第一轮优先验证：
  - 本地配置检查
  - 本地测试就绪判断
- 默认不改动任何文件

## 1. `novel-config`

### 测试目标

验证它能识别项目内缺少 `.env`，并结合 `~/.codex/config.toml` 给出合理的本地配置判断。

### 推荐提示词

```text
硬约束：只允许操作这个项目目录
/Users/arm/Desktop/vscode/Genm-codex

如果你当前上下文不是这个目录，先切换到这个目录，再继续。
如果不能确认自己正在读取这个目录，就停止，不要继续。

在确认目录正确后，再使用 novel-config skill，做一次 check。

要求：
1. 优先检查：
   - `.env`
   - `.mighty/config.json`
   - `~/.codex/config.toml`
2. 不要改动任何文件

完成后只汇报：
- 实际操作的项目根目录
- 本地配置状态摘要
- 主要缺口
- 下一步建议
- 是否改动了任何文件
```

## 2. `novel-test`

### 测试目标

验证它能基于本地配置判断“是否具备做连接测试的条件”，而不是假装已经完成真实联网测试。

### 推荐提示词

```text
硬约束：只允许操作这个项目目录
/Users/arm/Desktop/vscode/Genm-codex

如果你当前上下文不是这个目录，先切换到这个目录，再继续。
如果不能确认自己正在读取这个目录，就停止，不要继续。

在确认目录正确后，再使用 novel-test skill，判断当前本地配置是否已经具备做连接测试的条件。

要求：
1. 优先检查：
   - `.env`
   - `~/.codex/config.toml`
2. 如果你不能证明已经完成真实联网测试，就明确说“尚未真实测试”
3. 不要改动任何文件

完成后只汇报：
- 实际操作的项目根目录
- 当前是否具备测试前置条件
- 你的判断依据
- 下一步测试建议
- 是否改动了任何文件
```
